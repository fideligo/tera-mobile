<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!--
        The patient app talks to the API in every build, so INTERNET belongs here and not only in
        the debug manifest the Flutter template puts it in. CAMERA and HIGH_SAMPLING_RATE_SENSORS
        merge in from packages/tera_capture, so the app inherits them by depending on the plugin
        rather than by remembering to repeat them.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!--
        Android 13 (API 33) made notifications an opt-in runtime permission. Without this the
        daily check reminder is silently dropped on every modern handset — no error, no dialog,
        nothing scheduled. `NotificationService.requestPermission` asks for it; the platform
        returns null on 12 and below, where it is granted at install.

        Deliberately NOT here: SCHEDULE_EXACT_ALARM. Android 14 restricts it to alarms and timers
        a user explicitly set, and Play reviews it on that basis; a health reminder does not
        qualify. The reminder is scheduled inexactly instead, which costs a few minutes of drift
        and nothing else.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Re-registers the scheduled reminder after a restart; without it a reboot silently ends it. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!--
        `android:label` is the name under the icon on the home screen. It was the Flutter project
        name, which is a Dart identifier and not a brand: patients saw "tera_patient".

        Not touched, deliberately: `applicationId` and `namespace` in build.gradle.kts, which still
        read `id.tera.tera_patient`. Those are the app's identity to the OS and to Play, never
        shown to anyone, and permanent once published — changing one makes this a different app
        that installs alongside the old one rather than over it.
    -->
    <application
        android:label="TERA"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
            flutter_local_notifications' receivers. The boot receiver is what makes a daily
            reminder survive a restart: Android drops scheduled alarms on reboot, and without
            this the reminder stops firing the first time the phone is turned off overnight.
        -->
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver
            android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
